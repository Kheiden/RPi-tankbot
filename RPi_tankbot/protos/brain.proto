syntax = "proto3";

message Memory {
  optional string memory_name = 1;
  required int64 memory_number = 2; // Unique id for this memory
  required double memory_time =3; // Timestamp of the memory
  optional double location_x = 4;
  optional double location_y = 5;
  optional double location_z = 6;
}
